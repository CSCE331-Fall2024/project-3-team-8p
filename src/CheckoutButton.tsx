// CheckoutButton.tsx
import React from 'react';
import './CustomerViewTopButtons.css';  // Optional for styling

const CheckoutButton: React.FC = () => {
    return (
        <button className="black-button">
            Checkout
        </button>
    );
};

export default CheckoutButton;